name: Upload Data Github to S3

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws_access_key_id: AKIAUUAWIKAPDAS6UKGS
        aws_secret_access_key: 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
        aws_s3_bucket: s3-github-bucket
        aws-region: ap-south-1
